<% content_for :title, "New contact · Rolodex" %>

<p><%= link_to "← Contacts", contacts_path %></p>
<h1>New contact</h1>

<%= render "contacts/form", editor: @editor, url: contacts_path, method: :post, submit: "Create contact" %>
